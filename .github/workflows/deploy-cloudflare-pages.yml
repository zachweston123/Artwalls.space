## ============================================================================
## ⚠️  DEPRECATED — replaced by deploy.yml (combined Pages + Worker deploy)
## This workflow is disabled. Do not re-enable.
## ============================================================================
name: "[DEPRECATED] Deploy to Cloudflare Pages"

on:
  workflow_dispatch:  # Manual only — no auto triggers

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use deploy.yml instead."
