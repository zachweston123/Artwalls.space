# Frontend environment variables (Vite)
# Set this to your backend base URL (Stripe server or Bubble backend API)
VITE_API_BASE_URL=http://localhost:4242

# Supabase (frontend)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# SECURITY: Do NOT add any sensitive keys here (Stripe secret, service role, etc.)
# Admin authentication is handled server-side via /api/admin/verify.

# Optional: analytics, etc
# VITE_SENTRY_DSN=

# Google Places / Maps (frontend - public API key with domain restriction)
VITE_GOOGLE_MAPS_API_KEY=
