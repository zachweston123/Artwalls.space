# @deprecated — This patch is obsolete.
#
# Admin authentication is now handled entirely by the email-allowlist
# mechanism in worker/index.ts (isAdminUser + requireAdmin).
# The old SHA-256 password-hash flow has been removed.
#
# See SECURITY.md for the current admin-access model.
