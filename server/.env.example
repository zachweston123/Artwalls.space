# ============================================================================
# server/.env.example — Artwalls.space local development
# Copy to server/.env and fill in real values from your provider dashboards.
# NEVER commit the real .env file. See SECURITY.md for details.
# ============================================================================

# Core server
PORT=4242
APP_URL=http://localhost:5173
CORS_ORIGIN=http://localhost:5173

# Stripe API keys — get from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_REPLACE_ME
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME

# Stripe Billing Price IDs (create in Stripe → Products)
STRIPE_SUB_PRICE_STARTER=price_REPLACE_ME
STRIPE_SUB_PRICE_GROWTH=price_REPLACE_ME
STRIPE_SUB_PRICE_PRO=price_REPLACE_ME

# Supabase (server-side only) — get from Supabase Dashboard → Settings → API
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Subscription redirect URLs
SUB_SUCCESS_URL=http://localhost:5173/#/artist-dashboard?sub=success
SUB_CANCEL_URL=http://localhost:5173/#/artist-dashboard?sub=cancel

# Checkout redirect URLs
CHECKOUT_SUCCESS_URL=http://localhost:5173/#/purchase-success
CHECKOUT_CANCEL_URL=http://localhost:5173/#/purchase-cancel

# Connect onboarding redirect URLs
CONNECT_REFRESH_URL=http://localhost:5173/#/dashboard
CONNECT_RETURN_URL=http://localhost:5173/#/dashboard

# Platform fee by artist subscription tier (basis points, 10000 = 100%)
FEE_BPS_FREE=2000
FEE_BPS_STARTER=1500
FEE_BPS_PRO=1000
FEE_BPS_ELITE=500